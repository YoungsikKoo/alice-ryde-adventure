class Inventory{constructor(max){this.maxSlots=max||16;this.items=[]}addItem(item,qty){const q=qty||1;const ex=this.items.find(s=>s.item.name===item.name);if(ex){ex.quantity+=q;return true}if(this.items.length>=this.maxSlots)return false;this.items.push({item:{...item},quantity:q});return true}removeItem(name,qty){const q=qty||1;const i=this.items.findIndex(s=>s.item.name===name);if(i===-1)return false;this.items[i].quantity-=q;if(this.items[i].quantity<=0)this.items.splice(i,1);return true}hasItem(name){return this.items.some(s=>s.item.name===name)}isFull(){return this.items.length>=this.maxSlots}}